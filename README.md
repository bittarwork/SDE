# SDE601 — تطبيق مراجعة تنقيب البيانات

تطبيق ويب تفاعلي لمراجعة مادة **SDE601 (Data Mining / تنقيب البيانات)** استعداداً للامتحان. يعمل محلياً في المتصفح بدون خادم أو اتصال إنترنت بعد فتح الملف.

---

## فكرة التطبيق

الهدف هو جمع **كل أدوات المراجعة في مكان واحد**:

| الأداة | الغرض |
|--------|--------|
| **التلخيص** | شرح منظم لكل فصل مع جداول، ملاحظات، وتمارين |
| **المخططات** | مخططات مفاهيمية لكل فصل + مخطط يربط المادة كاملة |
| **الكويزات** | أسئلة كتاب + امتحان + وضع اختبار تفاعلي |
| **البطاقات** | مراجعة سريعة (سؤال ↔ جواب) |
| **تمارين تفاعلية** | حسابات خطوة بخطوة (Apriori، Entropy، K-Means، …) |

التطبيق **أحادي الصفحة (SPA)** — المحتوى والمنطق في JavaScript، والواجهة في HTML/CSS. التقدم يُحفظ تلقائياً في **localStorage** للمتصفح.

---

## هيكل الملفات

```
تنقيب بيانات/
├── index.html               ← نقطة الدخول — افتح هذا الملف
├── study-app.js             ← المحتوى الكامل + منطق التطبيق
├── concept-maps.js          ← المخططات المفاهيمية (فصل + مادة)
├── .nojekyll                ← يمنع معالجة Jekyll على GitHub Pages
└── README.md                ← هذا الملف
```

---

## التشغيل

1. افتح `index.html` في أي متصفح حديث (Chrome، Edge، Firefox، Safari).
2. لا حاجة لتثبيت Node.js أو npm.
3. للموبايل: انسخ المجلد للجهاز وافتح الملف من متصفح الجوال، أو استضف المجلد على أي خادم ثابت.

---

## النشر على GitHub Pages

1. أنشئ مستودعاً جديداً على GitHub (مثلاً `data-mining-review`).
2. من داخل مجلد المشروع نفّذ الأوامر التالية (بدّل الرابط برابط مستودعك):

```bash
git init
git add .
git commit -m "Data mining study app"
git branch -M main
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin main
```

3. من صفحة المستودع على GitHub: **Settings → Pages**.
4. تحت **Build and deployment** اختر **Source: Deploy from a branch**، ثم **Branch: `main` / `(root)`** واحفظ.
5. انتظر دقيقة، وسيظهر رابط الموقع: `https://<username>.github.io/<repo>/`.

---

## الفصول المغطاة

| الفصل | الموضوع |
|-------|---------|
| 1 | مقدمة، KDD، مصادر البيانات، وظائف التنقيب |
| 2 | معالجة البيانات (تنظيف، تكامل، تحويل، تقليل) |
| 3 | مستودع البيانات، OLTP/OLAP، ETL |
| 4 | مكعب البيانات وعمليات OLAP |
| 5 | قواعد الارتباط وخوارزمية Apriori |
| 6 | التصنيف (شجرة قرار، بايز، SVM، …) |
| 7 | السلاسل الزمنية والانحدار |
| 8 | العنقدة (K-Means، Hierarchical، DBSCAN) |
| 9 | العنقدة متعددة الأبعاد (CLIQUE، PROCLUS، …) |

---

## واجهة الاستخدام

### شريط الفصول
أزرار دائرية للتنقل بين الفصول 1–9. على الشاشات الصغيرة يظهر **رقم الفصل فقط** (مع الاسم للفصل النشط)، مع تمرير أفقي.

### التبويبات الرئيسية

- **📖 التلخيص** — المحتوى التعليمي الكامل مع قائمة جانبية للأقسام.
- **🗺️ مخططات** — مخطط الفصل الحالي + مخطط المادة + KDD + وصفي/تنبؤي.
- **❓ الكويزات** — وضعان:
  - **اختبار تفاعلي**: إجابات، تصحيح فوري، نتيجة نهائية.
  - **مراجعة**: إظهار/إخفاء الإجابات يدوياً.
- **🃏 بطاقات** — اضغط لقلب البطاقة.

### أدوات إضافية

- **بحث** — فلترة أقسام التلخيص أو أسئلة الكويز.
- **☰ القائمة** (موبايل/تابلت) — فتح التنقل السريع الجانبي.
- **🖨 طباعة** — طباعة المحتوى (يخفى الشريط والقوائم).

---

## اختصارات لوحة المفاتيح

| المفتاح | الإجراء |
|---------|---------|
| `1` – `9` | الانتقال للفصل |
| `M` | فتح تبويب المخططات |

---

## حفظ التقدم

يُخزَّن في `localStorage` تحت المفتاح `sde601_study_v28`:

- الفصل والتبويب الحاليين
- وضع الكويز (اختبار / مراجعة)
- إجابات الاختبار التفاعلي
- تقدم المراجعة وبطاقات التمارين

> مسح بيانات المتصفح يحذف التقدم.

---

## التصميم المتجاوب

التطبيق مُحسَّن لـ:

- **الموبايل**: شبكة أدوات، تمرير أفقي للفصول والتبويبات، أزرار لمس 44px+، دعم `safe-area` للشاشات ذات النتوء.
- **التابلت**: توازن بين الشريط والمحتوى.
- **الكمبيوتر**: شريط جانبي ثابت، عرض أوسع (حتى 1320px)، خطوط أوضح.

ارتفاع الهيدر يُحسب ديناميكياً لضبط التمرير والقائمة الجانبية.

---

## التقنيات

- HTML5 + CSS3 (متغيرات، Grid، Flexbox، Media Queries)
- JavaScript (Vanilla — بدون إطار عمل)
- localStorage للحالة
- لا قاعدة بيانات ولا backend

---

## التطوير المستقبلي (اختياري)

- إضافة فصول PDF جديدة
- تصدير/استيراد التقدم
- وضع PWA للتثبيت على الجوال

---

## الترخيص والاستخدام

مشروع مراجعة شخصي/تعليمي لمادة SDE601. المحتوى مبني على المنهج والكتاب وأسئلة Telegram الرسمية.

**تاريخ آخر تحديث للوثائق:** 20/06/2026
